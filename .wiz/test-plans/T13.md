# T13 — a11y polish

## Acceptance criteria
- Global `:focus-visible` rule on every interactive control gives a visible accent outline.
- Icon-only buttons have aria-label; decorative spans are aria-hidden.
- RolePill foreground vs `#161c28` hits WCAG AA ≥4.5:1 for all four roles.
- App has exactly one `<main>`; Sidebar is `<nav aria-label="primary">` (already); Header is `<header>` (already).
- ToastHost has role="status" aria-live="polite".

## Test scenarios (vitest)
1. Inline contrast helper: `contrastRatio('#e6edf7','#161c28')` returns ≥4.5.
2. Each role pill foreground (after tokens.css adjustments) hits ≥4.5:1 on `#161c28`.
3. CopyButton renders with a non-empty aria-label.
4. ToastHost has role="status" and aria-live="polite".
5. Sidebar nodes pulse has aria-label "fleet status: …" (already).
6. App layout has exactly one `<main>` element.
