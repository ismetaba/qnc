# T10 — Users, Billing, Settings

## Acceptance criteria
- Users page (admin-only nav): table from /v1/users with avatar initials circle, name, email, RolePill, capabilities blurb.
- Billing: MTD stat card + ProgressBar (requests / 80M included) + estimated cost + 6 invoices.
- Settings (admin-only nav): read-only facts (QNC Demo, Discovery, us-east-1, build sha or 'dev').
- /users, /billing, /settings hidden from sidebar when role lacks them.
- Switching billing → hides Endpoints/RPC/Nodes/Users/Settings; switching back → restores all 8.

## Test scenarios
1. UsersPage renders one row per user with initials avatar + RolePill + capabilities blurb.
2. BillingPage renders the MTD card with progress bar + estimated cost matching plan formula.
3. BillingPage renders 6 invoice rows.
4. SettingsPage shows the 4 workspace facts (QNC Demo, Discovery, us-east-1, build sha).
5. Sidebar billing role: only ['overview','billing']; admin restored to all 8.
6. /users route under billing role redirects (NavGuard fallback).
