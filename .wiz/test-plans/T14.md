# T14 — User-story e2e coverage + final QC sweep

## Acceptance criteria
- 4 new/extended tests covering user stories #2, #3, #5, #7.
- `pnpm -r test` green.
- No stray `console.log` in production source under `apps/*/src/**` (excluding boot-time logs / shutdown logs / `eslint-disable no-console` comments).
- Every package.json has `"type": "module"`; root tsconfig.base.json has `strict: true`.

## Test scenarios
1. **US-2 admin create endpoint**: render `<EndpointsPage/>` as admin, mock POST returning a new endpoint, fill name+chain, click submit, assert the new row appears with a copy button targeting `/api/v1/rpc/<new-id>`.
2. **US-3 playground → metrics within 1 tick**: spin up `buildApi()` with stubbed proxy fetch, `app.inject` POST `/v1/rpc/:id`, then `app.inject` GET `/v1/metrics?endpointId&windowMs=60000`, assert requestCount === 1.
3. **US-5 billing sidebar exact set**: `Sidebar` rendered with role=billing has links exactly `['overview','billing']`, none of the others.
4. **US-7 metrics window pills**: render `<MetricsPage/>` mocking `/v1/endpoints` + `/v1/metrics`, click each of 1m/5m/1h/24h, assert the corresponding fetch URL contains `windowMs=60000 / 300000 / 3600000 / 86400000`.
